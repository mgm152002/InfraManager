fastapi==0.100.0  # Ensure compatibility with the FastAPI version
uvicorn==0.23.0  # ASGI server for running the FastAPI app
python-dotenv==1.0.0  # For loading environment variables from .env
digitalocean==1.18.0  # For interacting with DigitalOcean's API